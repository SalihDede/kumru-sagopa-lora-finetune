# RunPod Serverless Requirements for Sagopa Chatbot
# Base image already has PyTorch 2.1.0 and CUDA 11.8

# Core ML libraries (NO torch - already in base image)
# Pin transformers to version compatible with PyTorch 2.1.0
transformers==4.40.0
tokenizers==0.19.1
accelerate==0.30.0
peft==0.10.0

# RunPod SDK
runpod

# Hugging Face Hub
huggingface-hub

# Additional utilities
safetensors
sentencepiece
protobuf
